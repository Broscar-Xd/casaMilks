# SQLite (local dev)
DATABASE_URL="file:./dev.db"

# PostgreSQL (producción)
# DATABASE_URL="postgresql://user:password@localhost:5432/casamilks"
JWT_SECRET="your-secret-key"
JWT_EXPIRES_IN="8h"
PORT=3000
NODE_ENV=development
