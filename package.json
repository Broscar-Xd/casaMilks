{
  "name": "casamilks",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci --ignore-scripts --include=dev && npx prisma generate && npx tsc && cd ../frontend && npm ci --include=dev && npm run build",
    "start": "cd backend && npx prisma db push --accept-data-loss 2>&1 && npx tsx prisma/seed.ts 2>&1 && node dist/index.js 2>&1"
  }
}
